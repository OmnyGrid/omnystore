/// The canonical OmnyStore package version (kept in sync with `pubspec.yaml`).
///
/// This is the single source of truth for "what build is this": it is rendered
/// in the CLI banner, reported by `GET /health`, and sent as the client SDK's
/// default `user-agent`.
const String omnyStoreVersion = '1.0.0';

/// The REST API version segment served by the API server (`/api/v1`).
const String omnyStoreApiVersion = 'v1';
