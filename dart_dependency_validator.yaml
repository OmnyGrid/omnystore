# Configuration for `dart run dependency_validator`.
#
# `meta` is used for `@immutable` annotations that the analyzer reads at compile
# time; the validator can miss such usages, so pinned deps are allowed here.
allow_pinned_deps: true

exclude:
  - "lib/**/*.g.dart"
